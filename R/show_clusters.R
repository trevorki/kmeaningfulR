# author: Hazel Jiang
# date: 2021-03-06
#
# Module to plot clusters by colour
#

#' This functionreduces a data set to 2 dimensions using principle component analysis (PCA) and colours clusters of points.
#'
#' @param X DataFrame Unprocessed data
#' @param clusters vector corresponding cluster for X
#' @param centroids array Center coordinate for each cluster
#'
#' @return plot A 2d principle components scatter plot coloured by cluster
#' @export
#'
#' @examples
#'  X = rbind(c(0,0), c(1,0), c(10,0), c(11,0))
#' centers = rbind(c(0, 0), c(10, 10))
show_clusters <- function(X, clusters, centroids = None){

}# Insert code here
